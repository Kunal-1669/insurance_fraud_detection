"""Top-level package for the project.

This file makes `src` importable as a Python package so that
relative imports like `from ..utils.config import Config` work when
modules are executed with `python -m src.<module>`.
"""

